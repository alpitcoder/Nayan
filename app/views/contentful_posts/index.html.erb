<h1>Contentful Posts</h1>

<ul>
  <% @posts.each do |post| %>
  <% if post.fields[:photo] != nil %>
    <li>
      <%= post.fields[:title]%>
      <%= post.fields[:photo].fields[:url] %>
      <%= link_to "Detail", contentful_post_path(post.id) %>
    </li>
  <% end %>
  <% end %>
</ul>
